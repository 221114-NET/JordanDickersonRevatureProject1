using RepoLayer;
using BusinessLayer;
using ModelsLayer;
namespace unitTest;

/*public class UnitTest1
{
    [Fact]
    public void SignUpTest()
    {
        // dependency mocking
        
        // Arrange
        IRepoClass mockRepo = new MockingLayer();
        IBusinessClass mockBusiness = new BusinessClass(mockRepo); // using the mock repo layer to test
        Employee e = new Employee();

        // ACT
        e = mockBusiness.SignUpRequest(e);
        // Assert
        Assert.Equal(e,e);
    }

    [Fact]
    public void LoginInTest()
    {
        IRepoClass mockRepo = new MockingLayer();
        IBusinessClass mockBusiness = new BusinessClass(mockRepo);
        List<Employee> e = new List<Employee>();

        e = mockBusiness.LoginRequest();

        Assert.Equal(e, e);
    }
}*/